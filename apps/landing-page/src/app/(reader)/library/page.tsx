import { DiscoverExperience } from '@/components/read/DiscoverExperience';

// Member Discover screen. The shared experience also powers the logged-out
// playground at /read (guest mode) — see components/read/DiscoverExperience.
export default function DiscoverPage() {
  return <DiscoverExperience />;
}
